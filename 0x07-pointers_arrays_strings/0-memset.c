#include "main.h"
/**
 * _memset - a function that fills memory with constant bytes
 * @s: location to fill
 * @c: char to fill location with
 * @numb: number of bytes to fill
 * Return: returns pointer to location filled
*/
char *_memset(char *s, char c, unsigned int numb)
{
	char *start = s;

	while (n--)
	{
		*s = c;
		s++
	}
	return (start);
}
